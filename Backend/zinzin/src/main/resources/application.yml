spring:
  application:
    name: zinzin
  config:
      import: application-secret.yml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${mysql.host}:${mysql.port}/${mysql.database}?useUnicode=true&serverTimezone=Asia/Seoul
    username: ${mysql.username}
    password: ${mysql.password}
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        globally_quoted_identifiers: true
        format_sql: true
        show_sql: true
    hibernate:
      ddl-auto: update
    open-in-view: true

  neo4j:
    uri: bolt://${neo4j.host}:${neo4j.port}
    authentication:
      username: ${neo4j.username}
      password: ${neo4j.password}

  jwt:
    token:
      secret-key: mySecr3tK3yWith$pec!alChar@cter$
    access:
      expiration: 600000
    refresh:
      expiration: 21600000

kakao:
  client:
    id: 605b8f5958cd7c03b1f456fda77ab473
    secret: QKFFcXfKwgwIeBQ1u28DBbPETFCKcNNw
    redirect-uri: http://localhost:8080/api/oauth2/kakao/callback
  auth-url: https://kauth.kakao.com/oauth/authorize
  token-url: https://kauth.kakao.com/oauth/token
  user-info-url: https://kapi.kakao.com/v2/user/me
#  friends-url: https://kapi.kakao.com/v1/api/talk/friends
  grant-type: authorization_code
  scope: profile_nickname,account_email,openid